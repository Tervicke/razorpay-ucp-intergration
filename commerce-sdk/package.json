{
  "name": "commerce-sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspace=@commerce-sdk/catalog",
    "typecheck": "npm run typecheck --workspace=@commerce-sdk/catalog",
    "test": "npm run test --workspace=@commerce-sdk/catalog"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
