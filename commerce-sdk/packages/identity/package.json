{
  "name": "@commerce-sdk/identity",
  "version": "0.1.1",
  "description": "Transport-independent agent identity and magic-link authentication",
  "type": "module",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" }
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepack": "npm run build",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run tests"
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
