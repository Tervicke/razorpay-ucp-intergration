import {connectMcp,structured} from "./mcp-client"; const base=(process.argv[2]||process.env.PUBLIC_BASE_URL||"http://localhost:3000").replace(/\/$/,"");
console.log(`Connecting to:\n${base}/api/ucp/mcp\n`);const c=await connectMcp(`${base}/api/ucp/mcp`);const tools=await c.listTools();console.log("Available tools:\n");tools.tools.forEach(t=>console.log(`✓ ${t.name}`));console.log('\nSearching:\n"black oversized shirt under ₹1500"\n');const search=structured(await c.callTool({name:"search_catalog",arguments:{catalog:{query:"black oversized",filters:{price:{max:150000}},pagination:{limit:10}}}}));search.products.forEach((p:any,i:number)=>console.log(`${i+1}. ${p.title}\n   ₹${p.price_range.min.amount/100}\n   Sizes: ${p.options[0].values.map((x:any)=>x.value).join(", ")}\n   ${p.variants.some((v:any)=>v.available)?"In stock":"Unavailable"}`));if(search.products[0]){const detail=structured(await c.callTool({name:"get_product",arguments:{catalog:{id:search.products[0].id}}}));console.log(`\nget_product:\n✓ ${detail.product.title} (${detail.product.variants.length} variants)`)}await c.close();
