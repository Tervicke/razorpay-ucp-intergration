import {connectMcp} from "./mcp-client";const base=(process.argv[2]||"http://localhost:3000").replace(/\/$/,"");console.log("Discovering merchant:\nTervicke Shop\n");const agents=await fetch(`${base}/agents.md`);if(!agents.ok)throw new Error("agents.md unavailable");const text=await agents.text();console.log("agents.md:\n✓ found\n");const match=text.match(/UCP discovery: (https?:\/\/\S+)/);if(!match)throw new Error("UCP URL was not discoverable from agents.md");console.log(`Discovered UCP URL:\n${match[1]}\n`);const response=await fetch(match[1]);if(!response.ok)throw new Error("UCP profile unavailable");const profile:any=await response.json();console.log("UCP:\n✓ found\n\nCapabilities:\n");Object.keys(profile.ucp.capabilities).forEach(x=>console.log(`✓ ${x.replace("dev.ucp.shopping.","")}`));const service=profile.ucp.services["dev.ucp.shopping"].find((x:any)=>x.transport==="mcp");if(!service)throw new Error("MCP service not advertised");console.log(`\nMCP transport:\n✓ ${service.endpoint}\n`);const client=await connectMcp(service.endpoint);const tools=await client.listTools();console.log("MCP tools:\n");tools.tools.forEach(t=>console.log(`✓ ${t.name}`));await client.close();
