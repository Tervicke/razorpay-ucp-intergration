import {catalogService} from "@/lib/catalog/catalog-service"; import {ProductCard} from "@/components/product-card"; export const metadata={title:"All products"}; export default function Products(){return <main className="section"><p className="kicker">Edition 01</p><div className="section-head"><h2>All products</h2><span>08 considered essentials</span></div><div className="grid">{catalogService.all().map(p=><ProductCard key={p.id} product={p}/>)}</div></main>}
