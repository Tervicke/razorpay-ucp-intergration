import type {Product,ProductVariant} from "@/lib/catalog/types"; import {baseUrl} from "@/lib/config"; import {UCP_VERSION} from "./profile";
const price=(amount:number)=>({amount,currency:"INR" as const});
const variant=(v:ProductVariant)=>({id:v.id,sku:v.sku,title:Object.values(v.options).join(" / "),price:price(v.price),selected_options:Object.entries(v.options).map(([name,value])=>({name,value})),available:v.availableQuantity>0,available_quantity:v.availableQuantity});
export function toUcpProduct(p:Product){const prices=p.variants.map(v=>v.price);return {id:p.id,handle:p.handle,title:p.title,description:{plain_text:p.description},url:`${baseUrl()}/products/${p.handle}`,categories:[{name:p.category}],price_range:{min:price(Math.min(...prices)),max:price(Math.max(...prices))},media:[{type:"image",url:`${baseUrl()}${p.imageUrl}`,alt_text:p.title}],options:[{name:"Size",values:[...new Set(p.variants.map(v=>v.options.Size))].map(value=>({value}))},{name:"Color",values:[{value:p.color}]}],variants:p.variants.map(variant),tags:p.tags};}
export const ucpMeta=(capabilities:string[])=>({version:UCP_VERSION,status:"success",capabilities:Object.fromEntries(capabilities.map(c=>[c,[{version:UCP_VERSION}]]))});
