import {beforeAll,describe,expect,it} from "vitest"; import fs from "node:fs"; import path from "node:path";
const db=path.resolve("/tmp/tervicke-shop-test.db"); beforeAll(()=>{process.env.DATABASE_PATH=db;try{fs.unlinkSync(db)}catch{}}); const {catalogService}=await import("../src/lib/catalog/catalog-service");
describe("CatalogService",()=>{it("searches deterministically",()=>expect(catalogService.search({query:"black oversized"}).products[0].title).toBe("Oversized Black T-Shirt"));it("filters maximum price",()=>expect(catalogService.search({priceMax:100000}).products.every(p=>p.variants[0].price<=100000)).toBe(true));it("filters category",()=>expect(catalogService.search({category:"Hoodies"}).products).toHaveLength(1));it("looks up product ID",()=>expect(catalogService.lookup(["prod_01"])[0].handle).toBe("oversized-black-t-shirt"));it("looks up variant ID",()=>expect(catalogService.lookup(["prod_01_l"])[0].id).toBe("prod_01"));it("looks up SKU",()=>expect(catalogService.lookup(["TV-01-L"])[0].id).toBe("prod_01"));it("gets product",()=>expect(catalogService.get("prod_01")?.variants).toHaveLength(4));it("returns undefined for unknown product",()=>expect(catalogService.get("missing")).toBeUndefined())});
